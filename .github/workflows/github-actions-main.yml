name: Hello World
run-name: Hello world github
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: [self-hosted, linux]
    container:
     image: node:22.11-bookworm
     options: --cpus 2
    steps:
      - run: cd frontend
      - run: ls
